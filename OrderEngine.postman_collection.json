{
  "info": {
    "name": "Order Execution Engine (Mock)",
    "_postman_id": "b8c34d3a-8a1b-4f6f-9c1c-collection",
    "description": "Postman collection to test API + concurrent orders + WebSocket notes.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Create Order (execute)",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{BASE_URL}}/api/orders/execute",
          "host": [
            "{{BASE_URL}}"
          ],
          "path": [
            "api",
            "orders",
            "execute"
          ]
        },
        "body": {
          "mode": "raw",
          "raw": "{\n  \"tokenIn\": \"SOL\",\n  \"tokenOut\": \"USDC\",\n  \"amountIn\": 5\n}"
        },
        "description": "Submit a market order and receive orderId. Open WS at {{WS_URL}}/ws/:orderId"
      },
      "response": []
    },
    {
      "name": "Health",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{BASE_URL}}/health",
          "host": [
            "{{BASE_URL}}"
          ],
          "path": [
            "health"
          ]
        }
      }
    }
  ],
  "variable": [
    {
      "key": "BASE_URL",
      "value": "http://localhost:3000"
    },
    {
      "key": "WS_URL",
      "value": "ws://localhost:3000"
    }
  ]
}